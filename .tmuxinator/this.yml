# ~/.tmuxinator/this.yml

name: this
root: ~/

windows:
  - boo:
    - tmux rename-window -t boo "👻 "
    - tmux select-layout main-vertical
    - tmux split-window -d
    - tmux split-window -h "mutt; bash -i"
    - irssi; bash -i

  - baz:
    - tmux rename-window -t baz "😍 "
