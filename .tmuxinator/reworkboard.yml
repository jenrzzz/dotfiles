# ~/.tmuxinator/reworkboard.yml

name: reworkboard
root: ~/Source/apple/reworkboard

windows:
  - boo: tmux link-window -ks "this:0" -t boo
  - baz: tmux link-window -ks "this:1" -t baz
  - srv: rst; bash -i
  - try: 'true'
  - explain: 'true'
