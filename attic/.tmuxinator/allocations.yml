# ~/.tmuxinator/allocations.yml

name: allocations
root: ~/Source/apple/allocations

windows:
  - boo: tmux link-window -ks "this:0" -t boo
  - baz: tmux link-window -ks "this:1" -t baz
  - srv: foreman start -f Procfile.dev-server; bash -i
  - lol: 'true'
  - wtf: 'true'
