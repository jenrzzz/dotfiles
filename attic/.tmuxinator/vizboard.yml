# ~/.tmuxinator/vizboard.yml

name: vizboard
root: ~/Source/apple/vizboard/vizboard

windows:
  - boo: tmux link-window -ks "this:0" -t boo
  - baz: tmux link-window -ks "this:1" -t baz
  - srv: foreman start -f Procfile web; bash -i
  - lol: 'true'
  - wtf: 'true'
