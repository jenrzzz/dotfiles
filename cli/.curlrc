# When following a redirect, set the previous URL as the referer
referer = ";auto"

# (Dropped the fake "IE10 on Windows 7" user-agent — a global UA override is more
#  likely to break modern sites than help. Set --user-agent per-call if needed.)
