# Start fresh
export PATH="/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/sbin"

# Add `~/bin`
export PATH="$HOME/bin:$PATH"

# Add npm binaries if they're there
if [[ -d /usr/local/share/npm/bin ]]; then
    export PATH=$PATH:/usr/local/share/npm/bin
fi
