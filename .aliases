# vim: set ft=sh sw=4 sts=4 :
# Easier navigation: .., ..., ...., ....., ~ and -
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ~="cd ~" # `cd` is probably faster to type though
alias -- -="cd -"

# Enable aliases to be sudo’ed
alias sudo='sudo '

## SHORTCUTS
alias ass="cd ~/Source/apple"
alias b="bundle "
alias d="cd ~/Dropbox"
alias g="git"
alias h="history"
alias j="jobs"
alias m="mate ."
alias o="open"
alias oo="open ."
alias s="cd ~/Source"
alias v="vim"
alias vq="vim -u NONE"
alias vimq="vim -u NONE"

less_macro_path=$(locate "macros/less.vim" | head -1)
if [[ -f $less_macro_path ]]; then
    alias vless="vim -u $less_macro_path"
elif [[ -f /usr/share/vim/vim72/macros/less.vim ]]; then
    alias vless="vim -u /usr/share/vim/vim72/macros/less.vim"
else
    alias vless="less -R"
fi
unset less_macro_path

# Git shortcuts
alias gs='git status'
alias gds='git diff --stat'
alias gpom='git push origin master'
alias gfo='git fetch origin'
alias gpo='git push origin'
alias gm='git merge'
alias gc='git commit'
alias gca='git commit -a'
alias ga='git add'
alias sb='git checkout'

# Golang shortcuts
alias gor='go run'
alias goc='go build'

# tmux environments
alias dev="tmux_create_or_attach dev"

## FANCY ls
# use GNU ls if it exists and we're on OS X
command -v gls >/dev/null 2>&1 && alias ls="gls -F --color=auto"
command -v gdircolors >/dev/null 2>&1 && alias dircolors="gdircolors"

# List all files colorized in long format
alias l="ls -l"

# List all files colorized in long format, including dot files
alias la="ls -Fla"

# List long, but don't show hidden files
alias ll="ls -Fl"

# List only directories
alias lsd='ls -Fl | grep "^d"'

# I don't know why I make this typo so often
alias sl="ls"

## OS X administration and hacks
# Get OS X Software Updates, and update Homebrew, npm, and their installed packages
command -v softwareupdate >/dev/null 2>&1 && alias update='sudo softwareupdate -i -a; brew update; brew upgrade; npm update npm -g; npm update -g'

# IP addresses
if [[ ! -f /etc/redhat-release ]]; then
    alias ip="dig +short myip.opendns.com @resolver1.opendns.com"
fi
alias localip="echo -n 'en0: '; ipconfig getifaddr en0; echo -n 'en1: '; ipconfig getifaddr en1; echo ''"
alias ips="ifconfig -a | grep -o 'inet6\? \(\([0-9]\+\.[0-9]\+\.[0-9]\+\.[0-9]\+\)\|[a-fA-F0-9:]\+\)' | sed -e 's/inet6* //'"
alias routerip="netstat -rn | grep default | tr -s ' ' | cut -d ' ' -f 2"

# Enhanced WHOIS lookups
alias whois="whois -h whois-servers.net"

# Flush Directory Service cache
command -v dscacheutil >/dev/null 2>&1 && alias flush="dscacheutil -flushcache"

# Clean up LaunchServices to remove duplicates in the “Open With” menu
alias lscleanup="/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user && killall Finder"

# Recursively delete `.DS_Store` files
alias cleanup="find . -type f -name '*.DS_Store' -ls -delete"

# ROT13-encode text. Works for decoding, too! ;)
alias rot13='tr a-zA-Z n-za-mN-ZA-M'

# Empty the Trash on all mounted volumes and the main HDD
# Also, clear Apple’s System Logs to improve shell startup speed
alias emptytrash="sudo rm -rfv /Volumes/*/.Trashes; sudo rm -rfv ~/.Trash; sudo rm -rfv /private/var/log/asl/*.asl"

# Show/hide hidden files in Finder
alias show="defaults write com.apple.finder AppleShowAllFiles -bool true && killall Finder"
alias hide="defaults write com.apple.finder AppleShowAllFiles -bool false && killall Finder"

# Hide/show all desktop icons (useful when presenting)
alias hidedesktop="defaults write com.apple.finder CreateDesktop -bool false && killall Finder"
alias showdesktop="defaults write com.apple.finder CreateDesktop -bool true && killall Finder"

# URL-encode strings
alias urlencode='python -c "import sys, urllib as ul; print ul.quote_plus(sys.argv[1]);"'

# Merge PDF files
# Usage: `mergepdf -o output.pdf input{1,2,3}.pdf`
alias mergepdf='/System/Library/Automator/Combine\ PDF\ Pages.action/Contents/Resources/join.py'

# Disable Spotlight
alias spotoff="sudo mdutil -a -i off"
# Enable Spotlight
alias spoton="sudo mdutil -a -i on"

# PlistBuddy alias, because sometimes `defaults` just doesn’t cut it
alias plistbuddy="/usr/libexec/PlistBuddy"

## USEFUL SHENANIGANS
# Intuitive map function
# For example, to list all directories that contain a certain file:
# find . -name .gitattributes | map dirname
alias map="xargs -n1"
alias nogrep='grep -v grep'

# Trim new lines and copy to clipboard
alias c="tr -d '\n' | pbcopy"

# Count word frequencies of stdin
alias freqs="awk '{ for (i = 1; i <= NF; i++) freq[\$i]++ } END { for (word in freq) printf \"%d\t%s\n\", freq[word], word }'"

# Canonical hex dump; some systems have this symlinked
command -v hd > /dev/null || alias hd="hexdump -C"

# OS X has no `md5sum`, so use `md5` as a fallback
command -v md5sum > /dev/null || alias md5sum="md5"

# OS X has no `sha1sum`, so use `shasum` as a fallback
command -v sha1sum > /dev/null || alias sha1sum="shasum"

# Ring the terminal bell, and put a badge on Terminal.app’s Dock icon
# (useful when executing time-consuming commands)
alias badge="tput bel"

# Reload bash profile
alias reload="source $HOME/.bash_profile"

# Bundler shortcuts
if command -v bundle > /dev/null; then
    alias be="bundle exec"
    alias bs="bundle show"

# Rake shortcuts cause I'm a lazy bastard
    alias rakenewdb="echo 'Dropping database...' && be rake db:drop && echo 'Creating database...' && be rake db:create && echo 'Loading schema...' && be rake db:schema:load"
    alias rakemigrate="echo 'Running migrations...' && be rake db:migrate"
fi

# Rails shortcuts
if command -v rails > /dev/null; then
    alias ra="echo \"bundle exec'ing rails..\" && bundle exec rails"
    alias rg="echo \"bundle exec'ing rails..\" && bundle exec rails generate"
    alias rst="echo 'Starting server...' && bundle exec rails server thin"
    alias rc="echo 'Starting console...' && bundle exec rails console"
    alias railsprod="echo 'RAILS_ENV=production' && export RAILS_ENV=production"
    alias railsdev="echo 'RAILS_ENV=development' && export RAILS_ENV=development"
    alias railstest="echo 'RAILS_ENV=test' && export RAILS_ENV=test"
    alias railsstage="echo 'RAILS_ENV=staging' && export RAILS_ENV=staging"
fi

# View HTTP traffic
alias sniff="sudo ngrep -d 'en1' -t '^(GET|POST) ' 'tcp and port 80'"
alias httpdump="sudo tcpdump -i en1 -n -s 0 -w - | grep -a -o -E \"Host\: .*|GET \/.*\""

# One of @janmoesen’s ProTip™s
for method in GET HEAD POST PUT DELETE TRACE OPTIONS; do
    alias "$method"="lwp-request -m '$method'"
done

# Stuff I never really use but cannot delete either because of http://xkcd.com/530/
alias stfu="osascript -e 'set volume output muted true'"
alias pumpitup="osascript -e 'set volume 7'"
alias hax="growlnotify -a 'Activity Monitor' 'System error' -m 'WTF R U DOIN'"
alias where="hostname"
alias why="echo because..."

# Puppet
if [[ -d '/etc/puppet' ]] && [[ -f '/etc/centos-release' ]]; then
    alias bouncepup="sudo service puppet restart; sudo tail -f /var/log/messages"
fi

## Quick connections
# Apple
if ping -c1 -t1 appleweb.apple.com >/dev/null 2>&1; then
    alias zpupup='echo "Updating puppet repo on zecora.apple.com..." && ssh -At jenner@zecora.apple.com "cd /etc/puppet; sudo -E git pull origin master"'
    alias calamity='ssh -A jenner@calamity.apple.com'
    alias parasol='ssh -A jenner@parasol.apple.com'
    alias film='ssh -A jenner@film.apple.com'
    alias thunderlane='ssh -A spghw@thunderlane.apple.com'
    alias pyrelight='ssh -A jenner@pyrelight.apple.com'
    alias zecora='ssh -A jenner@zecora.apple.com'
fi

# SDFA
if ping -c1 -t1 dad1.sdfa.lan >/dev/null 2>&1; then
    alias sas2='ssh -A jenner@sas2.sdfa.lan'
fi

alias sdfa='ssh -A jenner@mail.sdfa.org'

# Other
alias lunaprox='ssh -AD 8080 jenner@luna.jfave.com'
alias luna='ssh -A jenner@luna.jfave.com'
