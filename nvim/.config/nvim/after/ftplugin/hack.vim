" Hack-specific after/ftplugin overrides. (Intentionally empty for now; kept as a
" hook so Meta Hack tweaks have a home. Only triggers on the `hack` filetype, which
" is registered by the Meta layer — inert on personal/macOS hosts.)
