# Only search case-sensitive if input is mixed case
--smart-case

# New types
--type-set=puppet=.pp
--type-set=plaintext=.md,.mdown,.mkdn,.textile,.rst,.txt
--type-set=erb=.erb,.rhtml

# Ignore log/
--ignore-dir=log/

# Ignore ctags
--ignore-file=is:tags
