# ~/.tmuxinator/this.yml

name: this
root: ~/

windows:
  - boo:
    - tmux rename-window -t boo "👻 "
    - tmux select-layout main-vertical
    - tmux split-window -d "mutt; bash -i"
    - htop; bash -i

  - baz:
    - tmux rename-window -t baz "😍 "
