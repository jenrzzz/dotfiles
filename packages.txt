# Linux packages for the dotfiles (apt/dnf names). The bootstrap installs these:
#   sudo apt-get install -y $(grep -vE '^\s*#|^\s*$' packages.txt)
# NOTE: `eza` is not in Ubuntu apt before 24.10, and `bat`/`fd` install under
# alternate binary names (batcat/fdfind) on Debian/Ubuntu — bootstrap.sh handles
# the eza release-binary install and the bat/fd symlinks.
git
tmux
curl
wget
gnupg
jq
# bc: tmux-powerline's weather segment uses it for its cache-freshness check; without
# it the check errors out and the segment refetches wttr.in on every status redraw.
bc
stow
direnv
ripgrep
fd-find
fzf
bat
zoxide
xclip
neomutt
fortune-mod
cowsay
