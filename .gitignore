# Compiled Python files
*.pyc

.virtualenv
.config/direnv

# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

.vim

# ctags
/tags
quickfix*.out

.yardoc

backend/functions/.envrc
backend/functions/firebase-admin-key.json
.node-version

firebase-admin-key.json
.envrc
.local

**/.claude/settings.local.json

# Secrets — materialized into $HOME by dot-secrets (see secrets/), never tracked.
# These are real files, not symlinks into the repo, so git can't pick them up;
# this block is belt-and-suspenders.
.config/fastmail/
.config/gh/hosts.yml
.config/github-copilot/
.config/shell/private.sh
.config/git/local
.mutt/certificates
.mutt/jenner.jfave.com
.mutt/swootrc
